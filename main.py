# main.py — Buildozer entry point
# Buildozer requires the entry file to be named main.py.
# This shim simply runs python.py so you never have to rename it.
from python import QuantumMatrixApp
QuantumMatrixApp().run()
